# portfolio

# lyonhuang.com (static)

Static site structure:

- `index.html` — home
- `work/` — case studies
- `assets/` — shared CSS

To view locally:

- Open `index.html` directly in your browser, or
- Serve a local static server from this folder (recommended for consistent relative links).

